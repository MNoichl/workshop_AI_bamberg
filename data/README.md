# Data

This folder is intentionally tracked so generated workshop datasets can be published in the repository and loaded back later via raw GitHub URLs.

The local OpenAlex export notebook writes the Bamberg University dataset here as:

- `data/bamberg_university_openalex_works.csv.gz`

Keep generated exports compressed when possible.
